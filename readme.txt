- The executable needs to be on the same folder as the .json files
- At the time, it only reads .json files
- Commands:
	* "-all": shows the word puzzle with all the words highlighted
	* $word$ -json: shows the json output of the typed word
	* $word$: shows the word highlighted on the console
	* -x: exits